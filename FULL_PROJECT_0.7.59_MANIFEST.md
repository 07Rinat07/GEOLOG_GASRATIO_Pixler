# Full project manifest — 0.7.59

Base: full 0.7.58 header/grid project.

Implemented:
- fixed missing `TabletTrackWidget._localizer`;
- passed active `TabletView` localizer into every rendered track;
- added pure source-contract and Qt integration regression tests;
- synchronized package version and RU/KK/EN project documents.

Project format: v20. Form schema: v6. Tablet layout: v16.
