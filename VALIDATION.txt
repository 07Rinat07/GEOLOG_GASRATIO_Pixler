GEOLOG GASRATIO@Pixler 0.7.3 validation
Date: 2026-07-21

Implemented:
- Open LAS creates a separate clean Well workspace.
- Previous well lithology, cuttings, LBA/calcimetry, stratigraphy, descriptions,
  interpretations and depth annotations are not attached to the new LAS.
- Common dataset activation path clears every well-scoped tablet layer.
- Rotated 0/+90/-90 text uses orientation-aware anchors and remains inside the
  selected interval for top/center/bottom placement.

Checks executed in this container:
- python -m compileall -q src: PASS
- tests/test_text_presentation.py: 3 passed
- tests/test_project_session_well_isolation.py: 2 passed
  (run with minimal PySide import stubs because PySide6 is not installed here)
- Python AST parsing for changed files: PASS

Limitations:
- Full GUI regression suite was not executed in this container because PySide6
  and the Qt runtime are unavailable.
- Windows smoke test should verify opening LAS A, entering test intervals, then
  opening LAS B: LAS B must be empty while LAS A remains available only under
  its own well node in the project tree.
