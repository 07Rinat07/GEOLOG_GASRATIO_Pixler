name: Disabled incomplete patch workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow intentionally disabled; replacement patch workflow follows."
