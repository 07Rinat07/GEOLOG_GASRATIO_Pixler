# GEOLOG GASRATIO@Pixler 0.7.58

## Исправлено

- Плотная шапка больше не заканчивается половиной последнего параметра: viewport содержит только полные строки по 58 px.
- Добавлен нижний безопасный зазор и построчная вертикальная прокрутка для колонок более чем с шестью параметрами.
- Экранные кривые получили сдержанный профиль: насыщенность уменьшается только при отображении, а обычные тонкие линии становятся легче в многокривых треках.
- Minor-grid стала слабее и автоматически скрывается, когда расстояние между линиями меньше читаемого пиксельного шага.
- Сохранённые цвета, настройки формы, PDF и печать не изменяются экранным профилем.
- Удалены устаревшие дубликаты import controllers из `ui`; используется services layer.

## Совместимость

Project format v20, form schema v6, tablet layout v16. Миграция не требуется.

## Проверка

19 focused tests; 1068 headless tests passed, 4 skipped; compileall passed. Визуальный Windows/PySide6 smoke-test обязателен.
