# GEOLOG GASRATIO@Pixler 0.7.54 — стабильные шапки и читаемые подсказки

- Переключатель `лин./лог.` удалён из рабочей шапки; тип шкалы меняется в полном окне настроек.
- Обычные и редактируемые шапки получили одинаковую фиксированную высоту.
- Смена scale mode больше не меняет геометрию формы и не создаёт пустой пояс.
- Линейка подписана как «Шкала · единица» и всегда показывает минимум и максимум.
- Промежуточные подписи добавляются адаптивно, а светлые цвета линейки затемняются для контраста.
- Hover, tooltip и карандаш показывают человекочитаемое имя параметра с мнемоникой.
- Исправлена белая заливка/невидимый текст QToolTip и служебных подписей.
- Package **0.7.54**; project format **v20**; form schema **v6**; tablet layout **v16**.
- Проверка: **67 focused passed**; **1061 headless passed, 4 skipped, 4 deselected**.
