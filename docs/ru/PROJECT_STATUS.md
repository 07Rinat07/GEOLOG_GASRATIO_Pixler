# Статус проекта

Срез: 24 июля 2026 года. Версия пакета: **0.7.53**. Project format: **v20**, form schema:
**v6**, tablet layout: **v16**.

## Завершено в 0.7.53

- инженерная линейка в шапке подписана единицей и режимом `лин./лог.`;
- линия шкалы и деления сделаны контрастнее, числовые подписи включают крайние значения;
- подпись линейки обновляется вместе с ручной единицей и типом шкалы;
- панель интервальной статистики использует уменьшенный шрифт, компактные строки и фиксированные
  числовые столбцы;
- длинное имя параметра выводится вместе с мнемоникой и единицей во второй строке;
- удалённые `PlotWidget` исключаются из depth navigation, cursor updates и event targets;
- stale plot во время `setYRange` журналируется и пропускается, не разрушая остальные колонки.

## Проверка

- focused scale/statistics/lifecycle suite: **49 passed**;
- доступная headless-регрессия: **1039 passed, 4 skipped, 3 deselected**;
- `compileall` выполнен;
- Windows/PySide6 smoke-test остаётся обязательным для визуальной шкалы, dock statistics и
  сценария form switch/scroll после редактирования.

## Следующий вертикальный срез

Read-only offline WITSML 2.1 inventory и mapping fixtures. ETP 1.2 остаётся заблокирован до
fixture replay.
