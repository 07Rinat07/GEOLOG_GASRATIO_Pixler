# Статус проекта

Срез: 24 июля 2026 года. Версия пакета: **0.7.55**. Project format: **v20**,
form schema: **v6**, tablet layout: **v16**.

## Завершено в 0.7.55

- общий пояс шапки сохранён для точного совпадения начала всех глубинных графиков;
- параметры каждой колонки размещаются подряд от верхнего края без растягивания промежутков;
- дополнительная высота поглощается нижним stretch после последнего параметра;
- плотные колонки продолжают прокручиваться внутри ограниченного пояса;
- устранён `NameError` при открытии универсального конструктора;
- добавлены Qt и статические regression-контракты top-packed компоновки.

## Проверка

Focused header/form/constructor-набор: **86 passed**. Доступная headless-регрессия: **1064 passed, 4 skipped, 4 deselected**. `compileall` выполнен. Windows/PySide6 smoke-test остаётся обязательным для визуальной проверки разных форм и DPI.

## Следующий вертикальный срез

Read-only offline WITSML 2.1 inventory и mapping fixtures.
