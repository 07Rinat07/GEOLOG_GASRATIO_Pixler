# Статус проекта

Срез: 24 июля 2026 года. Версия пакета: **0.7.48**, исправляющая тестовая сборка.
Project format: **v20**, form schema: **v6**, tablet layout: **v16**.

## Завершено в 0.7.48

- шапка обычной кривой стала полноценной инженерной шкалой;
- минимум и максимум видны по краям, а промежуточные подписи совпадают с основными линиями
  вертикальной сетки конкретной колонки;
- minor ticks используют те же промежуточные деления, что и экранная/печатная сетка;
- линейная и логарифмическая интерполяция подписей разделены;
- минимум/максимум можно подготовить одновременно и применить кнопкой `✓` или Enter;
- единица измерения и тип шкалы редактируются непосредственно в шапке;
- единица является display override и не пересчитывает данные;
- unit/range/scale/header colors сохраняются в tablet layout и пользовательской форме;
- layout v15 мигрирует в v16 без изменения старых единиц источника;
- исправления DB/LAS и диагностический центр 0.7.46–0.7.47 сохранены.

## Проверка

- focused header/form/import: **152 passed, 3 skipped, 3 deselected**;
- доступная headless-регрессия: **1020 passed, 4 skipped, 3 deselected**;
- `compileall` выполнен;
- Windows smoke-test реального Qt-render, узких колонок и HiDPI обязателен из-за отсутствия
  PySide6/pyqtgraph в контейнере.

## Следующий вертикальный срез

После подтверждения 0.7.48 на Windows план возвращается к read-only offline WITSML 2.1
inventory и mapping fixtures. ETP 1.2 остаётся заблокирован до fixture replay.
