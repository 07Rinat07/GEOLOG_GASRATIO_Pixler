# Статус проекта

Срез: 24 июля 2026 года. Версия пакета: **0.7.54**. Project format: **v20**, form schema:
**v6**, tablet layout: **v16**.

## Завершено в 0.7.54

- `лин./лог.` удалён из рабочей шапки и остаётся в полном окне настроек кривой;
- все типы параметров используют одну фиксированную высоту строки, поэтому смена шкалы не
  изменяет синхронизированную геометрию формы;
- инженерная линейка показывает «Шкала · единица», обязательные крайние значения и адаптивные
  промежуточные подписи;
- светлые пользовательские цвета автоматически затемняются только для линейки, не меняя кривую;
- tooltip графика и live-readout карандаша используют понятное имя и мнемонику;
- QToolTip получил явные контрастные цвета, а служебные подписи — прозрачный фон.

## Проверка

- focused header/tooltip/form suite: **67 passed**;
- доступная headless-регрессия: **1061 passed, 4 skipped, 4 deselected**;
- `compileall` выполнен;
- Windows/PySide6 smoke-test остаётся обязательным для визуальной шкалы, разных форм и DPI.

## Следующий вертикальный срез

Read-only offline WITSML 2.1 inventory и mapping fixtures. ETP 1.2 остаётся заблокирован до
fixture replay.
