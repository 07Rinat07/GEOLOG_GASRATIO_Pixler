# Текущий статус проекта

## Исправление 0.7.3 — изоляция LAS

### Исправления 0.7.4

- читаемые многострочные заголовки Excel: название, мнемоника и единица;
- лист `Parameters` с расшифровкой и статусом распознавания;
- RU/KK/EN и очистка ошибок кодировки LAS.


- Новый LAS открывается в отдельной чистой скважине.
- Геологические интервалы предыдущей скважины не переносятся.
- Вертикальный текст целиком остаётся внутри интервала.

Дата: 21 июля 2026

Текущий срез — **«Универсальный конструктор 0.7.2: точные BMP-литотипы»**, встроенный в реальный проект поверх
существующих Form Manager, GeoData-планшета и Masterlog renderer.

Готово:

- секции «Геология», «Технология» и «Газовые данные» по предоставленному рабочему экрану;
- общая шкала глубины и единая высота шапки всех колонок;
- рабочая библиотека из глубинки, Masterlog и временного инженерного контроля;
- создание, повторная правка и удаление литологии;
- единый редактор шламовой пробы, цветового ЛБА 1–5, кальциметрии с автоматическим остатком и форматированного описания;
- обновление существующей записи по прежнему `sample_id` без дубликатов;
- абсолютная газовая колонка `TG_CALC`, `C1`, `C2` и отдельная колонка относительного состава `C1_REL`–`C5_REL`;
- полностью пустой газовый отсчёт остаётся `NULL/NaN`, а не превращается в ложный ноль;
- адаптивное главное окно, горизонтальная прокрутка и сворачиваемый инспектор;
- корректные разрывы кривых на NULL/NaN;
- обычное сохранение по дискете/`Ctrl+S` и «Сохранить как» по `Ctrl+Shift+S`;
- документация и строки интерфейса RU/KK/EN.

Остаётся: объединить экранный и печатный конструкторы формы, завершить редактор шапки и
автоматическую интерпретацию Gas Ratio/Pixler.

## Редактируемые подписи и стратиграфия

Любая дорожка полностью редактируется по правой кнопке: название, раздел, ширина, ось X,
параметры, подписи, стили, шкалы и диапазоны. Стратиграфия получила проектный справочник и
рабочий ввод интервала через `Shift + ЛКМ`, отдельную кнопку панели, двойной щелчок и сохранение.
Инструкция: [Редактирование подписей и стратиграфия](FORM_CAPTIONS_AND_STRATIGRAPHY.md).

Проверка текущего среза: `932 passed, 1 skipped`; Ruff и compileall без ошибок.

## LAS Editor 2 — текущий статус

Реализованы безопасная копия убывающего LAS, прогрессивное сращивание, прямая вставка
внешних кривых, source manifest, защищённые Undo/Redo, карандаш, синхронный пересчёт и
Excel-подобная таблица с экспортом.

### Исправление графического карандаша (slice22)

Добавлен явный выбор исходной LAS-кривой, кнопка карандаша на главной панели, запуск из контекстного меню планшета, специальный курсор и предварительный оранжевый штрих. Расчётные кривые исключены из прямого редактирования; зависимые параметры пересчитываются после изменения исходных данных.

## Конструктор 0.7.1

Раздел встроен в приложение и использует существующие Form Manager и Masterlog renderer.
Ресурсы находятся внутри пакета, а не во внешнем overlay. Реализованы поиск и миниатюры,
WYSIWYG-шапка, страницы и ориентации, изображения, динамическая легенда, глубинные
обозначения со смещением X/Y и preflight. Инструкция: [Конструктор](CONSTRUCTOR.md).

## Версия 0.7.1: текст и литотипы

Направление 0°/±90° и положение у верха/по центру/у низа теперь сохраняются не только для
стратиграфии, но и для заголовков формы, текста/полей шапки и подписей литотипов. Все 117
переданных BMP-рисунков доступны в литологии, шламограмме и шапках через один справочник.
Заводскую запись можно переопределить и сбросить; пользовательскую — добавить и изменить.
Экран, preview, PDF и печать используют одну модель.

## Версия 0.7.2: точные BMP-литотипы

Стандартные породы теперь используют реальные рисунки из `Litol_Bmp(2).zip` и
`Litol2_Bmp(2).zip`, а не сплошной цвет. На планшете рисунок повторяется без растягивания при
любом масштабе глубины. Подписи поверх рисунка выключены по умолчанию и включаются отдельно в
редакторе формы. Эти же рисунки используются в шламограмме, легендах шапки, preview, PDF и
печати. Полный прогон: `988 passed, 1 skipped`.

## Запланированная единая отчётность

Текущий PDF кальциметрии/ЛБА будет расширен до общего интервального отчёта по литологии,
шламовым пробам, ручным описаниям, ЛБА, кальциметрии, стратиграфии, C1–C5, суммарному газу,
Gas Ratio/Pixler, H₂S/CO₂, технологическим параметрам и событиям. Планируются
PDF/DOCX/XLSX/CSV/TSV/HTML, preview и preflight. Автоматическое описание пород не допускается.
