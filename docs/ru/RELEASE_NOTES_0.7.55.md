# GEOLOG GASRATIO@Pixler 0.7.55 — параметры шапки прижаты к верхнему краю

- Общий синхронизированный пояс шапки сохранён для совпадения глубинных сеток всех колонок.
- Параметры внутри каждой колонки теперь идут подряд от верхней границы.
- Всё свободное место размещается только под последним параметром.
- Редкие колонки больше не растягивают пустые интервалы между шкалами.
- Плотные колонки сохраняют внутреннюю вертикальную прокрутку.
- Исправлен `NameError: opened_from_projection` при открытии конструктора; восстановление lag/depth dataset возвращено в правильный workflow.
- Package **0.7.55**; project format **v20**; form schema **v6**; tablet layout **v16**.

## Проверка

- focused: **86 passed**;
- headless: **1064 passed, 4 skipped, 4 deselected**;
- `compileall`: успешно;
- Windows/PySide6 visual smoke-test: требуется.
