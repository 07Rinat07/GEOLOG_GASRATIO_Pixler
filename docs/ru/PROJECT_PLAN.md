# План проекта

Актуально на 24 июля 2026 года. Hotfix **0.7.54** сохраняет project format v20, form schema v6
и tablet layout v16. Следующий предметный этап после Windows-подтверждения — read-only offline
WITSML 2.1 inventory и mapping fixtures.

## P0 — hotfix 0.7.54: стабильные шапки, читаемые шкалы и единые названия параметров

- [x] убрать переключатель `лин./лог.` из рабочей шапки и оставить его в полном окне настроек;
- [x] сделать высоту редактируемых и обычных шапок одинаковой и строго фиксированной;
- [x] исключить изменение геометрии шапки при смене линейной/логарифмической шкалы;
- [x] подписывать линейку как «Шкала · единица» без дублирования режима;
- [x] гарантированно показывать крайние значения и адаптивно добавлять промежуточные подписи;
- [x] усиливать контраст светлых цветов шкалы на белом фоне;
- [x] показывать в hover, tooltip и карандаше понятное имя параметра вместе с мнемоникой;
- [x] задать читаемую палитру QToolTip и прозрачный фон служебных подписей;
- [x] покрыть изменения source-contract и headless regression тестами;
- [ ] Windows/PySide6: проверить формы с разным числом кривых, ширину 80–400 px, смену
  `linear/logarithmic` через настройки, hover и DPI 100/125/150%.

Критерий выхода: смена масштаба не создаёт пустые пояса, все формы используют одинаковую
геометрию параметров, цифры шкалы различимы, а подсказки не превращаются в белые прямоугольники.

## Следующие этапы

- [ ] read-only offline WITSML 2.1 inventory и mapping fixtures;
- [ ] alignment-controlled multi-dataset overlays в одной форме;
- [ ] directory watcher с preview-подтверждением ежедневного прироста;
- [ ] secured ETP 1.2 только после успешного fixture replay.
