# План проекта

Актуально на 24 июля 2026 года. Hotfix **0.7.55** сохраняет project format v20,
form schema v6 и tablet layout v16.

## P0 — hotfix 0.7.55: параметры шапки прижаты к верхнему краю

- [x] сохранить единый общий пояс шапки, чтобы начала PlotWidget и глубинные сетки совпадали;
- [x] прижать список параметров каждой колонки к верхнему краю пояса;
- [x] направлять всё дополнительное место только под последний параметр;
- [x] исключить распределение свободной высоты между шкалами редких колонок;
- [x] сохранить вертикальную прокрутку для колонок, где параметров больше лимита пояса;
- [x] исправить `NameError: opened_from_projection` при открытии конструктора;
- [x] добавить Qt/source-contract тесты top-packed компоновки;
- [ ] Windows/PySide6: проверить формы с 1, 3 и 7+ параметрами при DPI 100/125/150%.

Критерий выхода: первый параметр начинается у верхней границы шапки, следующие идут без
промежутков, а неизбежная свободная область находится только снизу и не нарушает глубинное
выравнивание графиков.

## Следующие этапы

- [ ] read-only offline WITSML 2.1 inventory и mapping fixtures;
- [ ] alignment-controlled multi-dataset overlays в одной форме;
- [ ] directory watcher с preview-подтверждением ежедневного прироста;
- [ ] secured ETP 1.2 только после успешного fixture replay.
