# Примечания к выпуску 0.7.40 — DOCX и HTML

- добавлена общая document model для DOCX и автономного HTML;
- оба формата используют один resolved ReportDefinition, interval и Coverage;
- `0`, `—` и `#N/A` отображаются как разные инженерные состояния;
- DOCX является безопасным OOXML без макросов и внешних объектов;
- HTML не содержит внешних scripts/styles;
- output и Passport v4 записываются одной восстанавливаемой транзакцией;
- Passport подписывает фактические байты DOCX/HTML;
- добавлены действия меню и инструкция на русском языке;
- формат проекта остаётся v16.

Проверки: 73 passed целевых; доступная регрессия — 926 passed, 4 skipped, 3 LAS-сценария deselected.
