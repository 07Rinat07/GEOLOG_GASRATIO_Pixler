# Примечания к выпуску 0.7.39 — транзакция файлов отчёта

- output и Report Passport фиксируются одной восстанавливаемой filesystem-транзакцией;
- готовый файл получает SHA-256, размер, MIME-type и безопасное имя в Passport schema v4;
- незавершённый commit откатывается, а подтверждённый commit безопасно завершает очистку;
- лишние старые страницы продолжения удаляются внутри той же транзакции;
- Print Center, прямой PNG/SVG/PDF, CSV/XLSX, Masterlog и интерпретационный PDF используют общий сервис;
- добавлен инструмент `tools/recover_report_transactions.py`;
- формат проекта остаётся v16.

Проверки: 37 целевых; доступная регрессия — 915 passed, 4 skipped, 3 LAS tests deselected.
