# GEOLOG GASRATIO@Pixler 0.7.47 — DB импортын қалпына келтіру және тақырыптағы диапазон

2026 жылғы 24 шілдедегі түзету тесттік жинағы. Project format **v20** болып қалады.

## GeoScape / Paradox DB

- `DEPT/DEPTH/MD` мәндерінің аралас реті енді файлды жарамсыз етпейді;
- Import Review белсенді индекс бойынша тек **қабылданған көшірмені** сұрыптайды, бастапқы
  `.db` және loader-owned dataset өзгермейді;
- барлық индекстер мен қисықтар бір тұрақты жол перестановкасымен сұрыпталады;
- қайталанатын тереңдік пен үзілістер warning болып қалады, ал NULL индекс және тұрақты индекс
  импортты бұғаттайды;
- batch DB → LAS кесте mixed болса да `DEPT`, `DEPTH`, `MD`, `HOLEDEPTH`, `BITDEPTH` өрістерін
  басым таңдайды;
- әлсіз немесе тең generic кандидаттар профиль не қолмен растауды талап етеді;
- сақталған профиль қолданылады және экспорт көшірмесі LAS жазу/round-trip алдында сұрыпталады.

## Қисық тақырыбы

- minimum және maximum әр кәдімгі қисық тақырыбында тікелей өңделеді;
- `A` батырмасы автоматты диапазонды қайтарады;
- өзгерістер `TabletController` арқылы ағымдағы жұмыс пішінінде сақталады;
- логарифмдік шкала нөлден кіші не тең minimum-ды қабылдамайды;
- толық баптауда атау түсі мен төменгі сызық түсі бөлек беріледі;
- project format, form schema v6 және tablet layout v15 өзгермеді.

## Тексеру

- focused DB/import/form suite: **149 passed, 3 skipped, 3 deselected**;
- қолжетімді headless regression: **1012 passed, 4 skipped, 3 deselected**;
- `compileall` өтті, wheel 0.7.47 isolation-сыз жиналды;
- контейнерде `PySide6`, `pyqtgraph`, `lasio` жоқ, сондықтан нақты `D1174.db`, batch DB → LAS
  және тақырып өрістері Windows-та тексерілуі тиіс.
