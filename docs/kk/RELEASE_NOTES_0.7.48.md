# GEOLOG GASRATIO@Pixler 0.7.48 — қисық тақырыбындағы инженерлік шкала және бірлік

2026 жылғы 24 шілдедегі тесттік жинақ. Project format v20 және form schema v6 өзгермейді,
tablet layout v15-тен v16-ға көтеріледі.

## Параметр тақырыбы

- minimum және maximum тақырыпта тікелей өңделеді;
- негізгі және аралық бөліністері бар көрінетін түсті шкала сызығы қосылды;
- бөліністер нақты бағанның нормаланған тік торымен сәйкес келеді;
- аралық жазулар linear және logarithmic шкала үшін дұрыс есептеледі;
- екі шекті енгізіп, `✓` не Enter арқылы бірге қолдануға болады;
- `A` автоматты диапазонды қайтарады.

## Бірлік және шкала түрі

- өлшем бірлігі тақырыпта көрініп, сол жерде өңделеді;
- linear/logarithmic режимі де тақырыпта ауыстырылады;
- бірлік тек көрсету параметрі, сандық мәндерді қайта есептемейді;
- толық curve settings терезесінде де бірлік өрісі бар;
- бірлік, диапазон, шкала түрі, түстер және тор бөліністері жұмыс пішінінде сақталады;
- ескі tablet layout v15 `unit_override = null` арқылы v16-ға қауіпсіз көшеді.

## Тексеру

- focused header/form/import suite: **152 passed, 3 skipped, 3 deselected**;
- қолжетімді headless regression: **1020 passed, 4 skipped, 3 deselected**;
- `compileall` өтті;
- тар бағандар мен HiDPI үшін Windows/PySide6 visual smoke-test міндетті.
