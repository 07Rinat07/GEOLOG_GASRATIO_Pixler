# Build manifest — GEOLOG GASRATIO@Pixler 0.7.72

- Package version: `0.7.72`
- Project format: `v20`
- Form schema: `v8`
- Tablet layout: `v18`
- Root `README.md`: unchanged; SHA-256 `9c7054c8c7df6b3398ae2fc0fe3a37c9ca3edea27baba0d79744acfee12bca8f`
- Main interface fix: the main and F4 command rows are plain application-owned `QFrame` widgets, not `QToolBar` instances and not native `QMainWindow` toolbar rows
- Window safety boundary: explicit `640×480` top-level minimum plus monitor-work-area-capped `minimumSizeHint()`
- Catalog-symbol stored minimum geometry: `0.01×0.01` logical pixel; rendered as the thinnest visible device-pixel mark
- Catalog-symbol edit selection frame: at least `18×18` logical pixels without modifying saved geometry
- Normal annotation minimum geometry: `40×24` logical pixels
- Resolved numeric-index export: CSV/XLSX use the exact active DEPTH/TIME rows and neutral numeric boundary labels
- Full release test run: `1668 passed`, `10 skipped` in `59.29 s`; process exit code `0`
- Test runner: third-party pytest plugins disabled; completed Windows Qt suites terminate the isolated runner without native DLL teardown
- Documentation audit: `92` localized Markdown files per language and `1901` synchronized i18n keys
- `compileall`: passed for `src`, `tests`, `tools`, and `scripts`
- Ruff: passed for `src`, `tests`, `tools`, and `scripts`
- Mypy: release gate remains open; the current run reports `29` findings before a mypy `2.3.0` internal error
- Wheel: `geolog_gasratio_pixler-0.7.72-py3-none-any.whl` (`2,904,261` bytes); SHA-256 `ee627e892270c7b3fe243a796e6ae5524c3b9aa4c4077fa61a6bc91da2b28a25`
- Wheel build command: `python -m pip wheel . --no-deps --no-build-isolation`
- Manual Windows external-monitor, HiDPI, PDF, and physical-printer acceptance remains open in `PROJECT_PLAN.md`
