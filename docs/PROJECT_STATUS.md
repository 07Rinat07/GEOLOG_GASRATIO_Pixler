# Статус проекта

Срез: 24 июля 2026 года. Версия пакета: **0.7.58**. Project format: **v20**,
form schema: **v6**, tablet layout: **v16**.

## Завершено в 0.7.58

- исправлена причина частичного обрезания последнего параметра в плотных колонках;
- высота viewport шапки выравнивается по целому числу строк по 58 px;
- содержимое шапки имеет нижний безопасный зазор, поэтому последняя граница не уходит под график;
- для колонок более чем с шестью параметрами включается заметная построчная прокрутка;
- добавлен сдержанный экранный профиль цветов кривых без изменения сохранённых цветов и печати;
- обычные тонкие линии автоматически уплотняются по толщине в многокривых колонках;
- major/minor сетка стала нейтральнее, а minor-линии скрываются при недостаточном пиксельном шаге;
- удалены устаревшие дубликаты UI import controllers, рабочий импорт использует services layer;
- сохранены overlay-статистика интервала, A4-аудит формы и diagnostics предыдущего среза.

## Проверка

Focused header/screen-style/source-contract: **19 passed**. Доступная headless-регрессия:
**1070 passed, 4 skipped**. `compileall` выполнен. Windows/PySide6 smoke-test остаётся
обязательным для плотных шапок с 7–12 параметрами, колесной прокрутки над шапкой и визуального
сравнения сетки на ширине колонок 80/160/300 px.

## Следующий вертикальный срез

Read-only offline WITSML 2.1 inventory и mapping fixtures.
