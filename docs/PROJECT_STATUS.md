# Статус проекта

Срез: 24 июля 2026 года. Версия пакета: **0.7.58**. Project format: **v20**,
form schema: **v6**, tablet layout: **v16**.

## Завершено в 0.7.58

- исправление перенесено непосредственно в полное дерево проекта 0.7.56;
- статистика интервала всегда работает как floating overlay и не уменьшает планшет;
- геометрия ограничивается активным монитором, включая многомониторные координаты;
- overlay следует за перемещением и resize главного окна;
- X окна, кнопка «Очистить», смена формы и смена dataset удаляют старое выделение;
- форма не сохраняет синюю заливку и отчёт от предыдущей компоновки;
- добавлены 3 геометрических и 4 интеграционных contract-теста;
- A4-аудит ширины формы, рабочие формы, diagnostics и предыдущие исправления сохранены.

## Проверка

Focused overlay-набор: **27 passed**. Доступная headless-регрессия: **1048 passed, 4 skipped**. `compileall` для `src` и `tests` выполнен.
Полный Qt smoke-test требует Windows-окружение с PySide6/pyqtgraph.

## Следующий вертикальный срез

Read-only offline WITSML 2.1 inventory и mapping fixtures.
