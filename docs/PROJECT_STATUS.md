# Статус проекта

Срез: 24 июля 2026 года. Версия пакета: **0.7.47**, исправляющая тестовая сборка.
Project format: **v20**, form schema: **v6**, tablet layout: **v15**.

## Завершено в 0.7.47

- смешанный порядок глубинного индекса DB нормализуется в принятой копии без изменения источника;
- Import Review показывает действие сортировки и больше не блокирует исправимый `D1174.db`;
- пакетный DB → LAS использует явный DEPT/DEPTH/MD и сохранённый ручной профиль;
- неоднозначные слабые кандидаты по-прежнему требуют подтверждения;
- minimum/maximum каждой обычной кривой редактируются прямо в шапке и сохраняются в форме;
- auto-range, логарифмическая валидация, цвет названия и цвет линии шапки подключены к
  существующему `CurveDisplaySettings` без миграции проекта;
- диагностический центр импорта 0.7.46 сохранён.

## Проверка

- focused DB/import/form: **149 passed, 3 skipped, 3 deselected**;
- доступная headless-регрессия: **1012 passed, 4 skipped, 3 deselected**;
- `compileall` выполнен; wheel 0.7.47 собран;
- Windows smoke-test реального DB и Qt-редакторов обязателен из-за отсутствия PySide6,
  pyqtgraph и lasio в контейнере.

## Следующий вертикальный срез

После подтверждения 0.7.47 на Windows план возвращается к read-only offline WITSML 2.1
inventory и mapping fixtures. ETP 1.2 остаётся заблокирован до fixture replay.
