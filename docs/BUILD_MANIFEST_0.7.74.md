# Build manifest — GEOLOG GASRATIO@Pixler 0.7.74

- Package version: `0.7.74`
- Project format: `v20`
- Form schema: `v8`
- Tablet layout: `v18`
- Main increment: headless typed WITS Level 0 parser and per-record sequence diagnostics
- Shared pipeline: `Wits0StreamProcessor` performs incremental framing, parsing, and sequence QC for both live TCP and raw replay
- Immutable parser results: `Wits0ParsedFrame`, `Wits0ParsedField`, and structured `Wits0Diagnostic`
- Supported value kinds: `float`, `integer`, `text`, `date`, and `time`
- Raw preservation: original frame bytes, decoded text, original line, raw value, arrival UTC, and source reference remain available
- Diagnostic coverage: invalid line/identifier, decode failure, empty or malformed value, non-finite number, unknown record/item, duplicate field, and mixed-record frame
- Sequence coverage: missing, invalid, first, contiguous, duplicate, gap, and out-of-order states tracked independently per record number
- Live/replay equivalence: deterministic tests compare complete parsed outputs across different TCP/file chunk boundaries
- Capture integration: frame events now carry parsed frames; snapshot counters include fields, parser warnings/errors, unknowns, and sequence anomalies
- User interface: modeless capture monitor adds a parsed-fields tab and parser/sequence status counters
- Field profile: GeoScape GSWITS profile still contains `11` records and `105` vendor fields; real-stream confirmation remains required
- Localized interface catalogs: RU/KK/EN parity maintained
- Localized documentation: RU/KK/EN WITS0 guide, release notes, status, features, and plan updated together
- Targeted protocol/import/documentation test gate: `71 passed`, `2 skipped` in `0.57 s`
- Documentation audit: passed with `96` localized Markdown files per language and `1975` synchronized i18n keys
- `compileall`: passed for `src`, `tests`, `tools`, and `scripts`
- Full-suite attempt: collection is blocked in this minimal environment because PySide6 and pyqtgraph are not installed; this is recorded rather than reported as a pass
- Ruff: unavailable in the current environment; no claim of a Ruff pass is made
- Wheel: `geolog_gasratio_pixler-0.7.74-py3-none-any.whl` (`2,939,218` bytes); SHA-256 `f5703cee0300c80d955dd76587d6cd214f46b1e179108518c9d7c530db8db202`
- Wheel integrity, version metadata, parser module, profile, and localization package-data paths: passed
- Wheel build command: `python -m pip wheel . --no-deps --no-build-isolation`
- Qt runtime note: PySide6 is unavailable in the current Linux validation environment, so offscreen Qt runtime tests are skipped; static UI/thread-boundary tests remain enabled
- Dataset/UOM boundary: parser output is not yet committed to `AcquisitionSession`; Import Review remains the next increment
- Field acceptance remains open for real GSWITS traffic, header/sequence confirmation, Windows long-duration capture, rotation, reconnect, and parser comparison with vendor output
