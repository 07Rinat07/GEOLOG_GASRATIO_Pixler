# GEOLOG GASRATIO@Pixler 0.7.53 — readable rulers and compact statistics

- Header rulers now show display unit and linear/log mode inside the scale band.
- Baseline, ticks and endpoint/major values are more visible.
- Unit and scale edits update the ruler caption in place.
- Interval statistics use smaller fonts, compact rows and mnemonic/unit details.
- Deleted PlotWidget wrappers are skipped during cursor, wheel and depth-range updates.

Package **0.7.53**; project format **v20**; form schema **v6**; tablet layout **v16**.
Verification: **49 focused passed**; **1039 headless passed, 4 skipped, 3 deselected**;
`compileall` passed. Windows/PySide6 visual smoke testing remains required.
