# GEOLOG GASRATIO@Pixler 0.7.54 — stable headers and readable help text

- Removed the linear/log selector from the working header; scale mode stays in full settings.
- Gave plain and editable curve headers one identical fixed height.
- Scale changes no longer alter form geometry or create an empty synchronized band.
- Labelled the ruler as “Scale · unit” and always render minimum and maximum values.
- Added adaptive intermediate labels and contrast correction for pale ruler colours.
- Hover, tooltip and pencil readouts show readable parameter name plus mnemonic.
- Fixed white-filled or invisible QToolTip/helper text under Windows palettes.
- Package **0.7.54**; project format **v20**; form schema **v6**; tablet layout **v16**.
- Verification: **67 focused passed**; **1061 headless passed, 4 skipped, 4 deselected**.
